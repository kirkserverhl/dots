# bat

Simple `bat` configuration.

Current setup uses a static Gruvbox theme.

## Future

This should eventually follow the matugen theme (like dunst and waybar) so colors stay consistent with the rest of the system.

The current config is intentionally kept minimal.
