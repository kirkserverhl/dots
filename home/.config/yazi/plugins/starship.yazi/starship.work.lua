starship.yazi Starship prompt plugin for[Yazi](https://github.com/sxyazi/yazi
<https://github.com/Rolv-Apneseth/starship.yazi/assets/69486699/f7314687-5cb1-4d66-8d9d-    cca960ba6716> Requirements [Yazi](https://github.com/sxyazi/yazi[starship] https:
//github.com/starship/starship Installation bash y pack-a RolvApneseth/starship Manual sh# Linux MacO git clone https://github.com/Rolv-Apneset h/starship.yazi.git ~/.config/yazi/pluginsstarship.yazi# Windowsgit clone https github.com/ Rolv-Apneseth tarship.yazi.git  %AppDat\yazi\config\plugins\starship.yazionfig/yazi/init.lualua require

"starship"):setup()Make sure you have [starship](https://github.com/ Wtarshipstarship)installed and in your PATH Config Here is an example with all available    config options lua require
"starship" setup ({flags (such as filter,find and search This is recomended
    for starship themes which--are intended togo across the entire width of the terminalhide_flags false,Default false Whether to place flags after the starship prompt. False means the flag will be placed before the prompt.flagsafter_prompt = true,     Default: true Custom starship configuration file us config_file~/.config/starship starship.toml Defaultnil}) ##ExtraIf you use a`starship` theme with a background colour, it might look a bit to cramped on justthe oneline Yazi gives the header by default To fix this, you can add this to your init.lua<details summary>Click to expandsummary lua local old_build=Tab.build Tab.build  function(self,)local br function(c x, y) if x <= 0 or x  self._area.w -1 then return ui.Bar(ui.Bar.TOP):area(ui.Rect.default d return ui.Bar(uiBar.TOP) rea(uiRect({ x x,y = math.max(0, y),w = ya.clamp(0,self._area.w x1),h math.min(1,self._area.h),}))symbol(c)ndlocal c =self.chunkself._chunksc[1]:padding(ui.Padding.y(1)),c[2]:padding(ui.Padding(c[1].w 0 and 0 or 1 c[3].w > 0 and 0 or 1, 1, 1))c[3]:padding(ui.Padding.y(1)  local style THEME.manager.border_style self._base ya.list_merge(selfbaseorui.Border(ui.Border.ALL):area(self.area):type(ui.Border.ROUND:style(style),ui.Bar(ui.Bar.RIGHT):area(self._chunks[1]):style(style)ui.Bar(ui.Bar.LEFT):area(self._chunks[1]):style(stle),bar("┬",c[1].right1c[1].y),bar(┴,c[1].right1c[1].bottom 1),bar("┬",c[2].right,c[2].y),bar("┴",c[2].right,c[2].bottom1),}) old_build(self,) and </details> !NOTE]>This works by overriding our Tab.build  function somake sure this0 is the only place you're doing that in your config.For example,would be incompatible with the ull-border plugin(https://github.com/yazi-rs/plugins/tee/main/ full-boeder.yazi Thankks [\sxyazi] (https://github.com/sxyazi) For providing the code for this plugin and the demovdeo in this comment(https://github.com/sxyazi/yazi issues76issuecomment-1977082834)
