# VSCodium configuration
# Only User/ settings are tracked (caches excluded).

export requires=()

# VSCodium is usually installed via AUR or flatpak on Arch
export pacman_deps=()
export aur_deps=(vscodium-bin)  # adjust to your install method
