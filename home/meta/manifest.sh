# home/
# Primary "big home" stow package.
# Contains most of the user's dotfiles under one package for simplicity.

export requires=()

# System packages are declared in packages/install.sh and individual manifests.
export pacman_deps=()
export aur_deps=()
